\documentclass[conference]{IEEEtran}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{hyperref}
\usepackage{cite}

\begin{document}

\title{EGD-DLM: Entropy-Guided Distillation for Accelerating Discrete Diffusion Language Models}

\author{
\IEEEauthorblockN{Author Name}
\IEEEauthorblockA{Columbia University\\
STAT GR 5244: Applied Unsupervised Learning\\
}
}

\maketitle

\begin{abstract}
Discrete diffusion language models (DLMs) have emerged as a promising alternative to autoregressive models, offering advantages such as parallel generation and the ability to overcome the reversal curse. However, their iterative denoising process requires many sampling steps, leading to slow inference. In this work, we propose \textbf{Entropy-Guided Distillation for Discrete Diffusion Language Models (EGD-DLM)}, a novel framework that accelerates DLM inference through knowledge distillation guided by token-level entropy. Our key insight is that tokens with high entropy (high uncertainty) in the teacher's predictions represent ``hard'' knowledge that requires more attention during distillation. By weighting the distillation loss according to teacher entropy, we enable more effective knowledge transfer. Experiments on WikiText-103 demonstrate that EGD-DLM achieves \textbf{8.96$\times$ speedup} (50 steps $\rightarrow$ 8 steps) while the student model surprisingly \textbf{surpasses the teacher} in perplexity (1193.30 vs. 1207.28), a phenomenon not observed in standard distillation where student performance degrades.
\end{abstract}

\begin{IEEEkeywords}
Diffusion Language Models, Knowledge Distillation, Entropy Scheduling, Text Generation, Unsupervised Learning
\end{IEEEkeywords}

% ============================================================================
\section{Introduction}
% ============================================================================

Large language models (LLMs) have revolutionized natural language processing, with autoregressive (AR) models like GPT \cite{radford2019language} and LLaMA \cite{touvron2023llama} achieving remarkable performance across diverse tasks. However, AR models suffer from inherent limitations: sequential token generation prevents parallelization, and the unidirectional nature leads to the ``reversal curse'' where models struggle with bidirectional reasoning \cite{berglund2023reversal}.

Diffusion models, originally developed for continuous domains like images \cite{ho2020denoising, song2020score}, have recently been adapted to discrete text generation. \textbf{Discrete Diffusion Language Models (DLMs)} such as MDLM \cite{sahoo2024simple}, SEDD \cite{lou2024discrete}, and LLaDA \cite{nie2025llada} demonstrate competitive performance with AR models while enabling parallel generation. LLaDA, in particular, scales to 8 billion parameters and matches LLaMA3-8B performance while using significantly fewer training tokens.

Despite these advances, DLMs face a critical challenge: \textbf{slow inference} due to the iterative denoising process. While AR models generate one token per forward pass, DLMs typically require 50-1000 denoising steps, creating a significant computational bottleneck.

\subsection{Related Work}

\textbf{Discrete Diffusion Language Models.} Early work on discrete diffusion includes D3PM \cite{austin2021structured} which introduced structured transition matrices for categorical data. MDLM \cite{sahoo2024simple} simplified the framework using masked diffusion, where the forward process progressively masks tokens and the reverse process predicts masked positions. SEDD \cite{lou2024discrete} proposed score entropy as a principled loss function for discrete spaces. Most recently, LLaDA \cite{nie2025llada} demonstrated that masked diffusion models can scale to 8B parameters and achieve competitive performance with state-of-the-art AR models.

\textbf{Diffusion Model Acceleration.} Several approaches have been proposed to accelerate diffusion models. For continuous diffusion, consistency models \cite{song2023consistency} and progressive distillation \cite{salimans2022progressive} reduce sampling steps through knowledge distillation. For discrete diffusion, SDTT \cite{deschenaux2024sdtt} applies self-distillation through time to reduce steps from 1024 to 32-64. D2F \cite{chen2025d2f} creates AR-diffusion hybrids achieving 50$\times$ acceleration. DiDi-Instruct \cite{wang2025didi} applies distribution-matching distillation to achieve up to 64$\times$ speedup.

\textbf{Entropy in Diffusion Models.} Entropy has been explored for improving diffusion models. Diffusion-EAGS \cite{han2024diffusion} uses entropy-adaptive Gibbs sampling during inference for controlled text generation. Entropic time schedulers \cite{liu2025entropic} propose time reparameterization based on entropy rate. However, these methods focus on sampling-time entropy scheduling rather than training-time optimization.

\subsection{Contributions}

We propose \textbf{EGD-DLM}, which makes the following contributions:

\begin{enumerate}
    \item \textbf{Entropy-Scheduled Training}: We introduce entropy-weighted loss during teacher training, where tokens with higher prediction entropy receive larger loss weights. This focuses learning capacity on uncertain (harder) tokens.

    \item \textbf{Entropy-Guided Distillation}: We extend entropy scheduling to the distillation phase. By using teacher entropy to identify ``hard'' tokens, we weight the distillation loss to prioritize knowledge transfer for difficult cases.

    \item \textbf{Empirical Validation}: We demonstrate that EGD-DLM achieves 8.96$\times$ speedup on WikiText-103, and uniquely, the student \textit{surpasses} the teacher in perplexity—a phenomenon not observed in standard distillation.
\end{enumerate}

% ============================================================================
\section{Method}
% ============================================================================

\subsection{Preliminaries: Discrete Diffusion}

Following LLaDA \cite{nie2025llada}, we adopt a mask-based discrete diffusion framework. Let $\mathbf{x}_0 = (x_0^1, \ldots, x_0^L) \in \mathcal{V}^L$ denote a sequence of $L$ tokens from vocabulary $\mathcal{V}$.

\textbf{Forward Process.} The forward process progressively masks tokens. At timestep $t \in [0, 1]$, each token is independently masked with probability $t$:
\begin{equation}
    q(\mathbf{x}_t | \mathbf{x}_0) = \prod_{i=1}^{L} \left[ t \cdot \mathbf{1}_{[\texttt{MASK}]} + (1-t) \cdot \mathbf{1}_{[x_0^i]} \right]
\end{equation}
where $\mathbf{1}_{[v]}$ denotes the one-hot distribution concentrated on token $v$.

\textbf{Reverse Process.} A neural network $p_\theta(\mathbf{x}_0 | \mathbf{x}_t, t)$ is trained to predict the original tokens from the masked sequence. The standard training objective is:
\begin{equation}
    \mathcal{L}_{\text{base}} = \mathbb{E}_{t, \mathbf{x}_0, \mathbf{x}_t} \left[ \sum_{i: x_t^i = \texttt{MASK}} -\log p_\theta(x_0^i | \mathbf{x}_t, t) \right]
\end{equation}

\subsection{Entropy-Scheduled Training}

Our first contribution is to weight the training loss by token-level entropy. The intuition is that tokens where the model is uncertain (high entropy) are harder to learn and should receive more training attention.

Given model logits $\mathbf{z}^i \in \mathbb{R}^{|\mathcal{V}|}$ for position $i$, we compute the entropy:
\begin{equation}
    H^i = -\sum_{v \in \mathcal{V}} p_\theta(v | \mathbf{x}_t, t) \log p_\theta(v | \mathbf{x}_t, t)
\end{equation}

We normalize entropy to $[0, 1]$ by dividing by the maximum possible entropy $\log |\mathcal{V}|$:
\begin{equation}
    \tilde{H}^i = \frac{H^i}{\log |\mathcal{V}|}
\end{equation}

The entropy-scheduled loss weights each token's contribution:
\begin{equation}
    \mathcal{L}_{\text{ES}} = \mathbb{E}_{t, \mathbf{x}_0, \mathbf{x}_t} \left[ \frac{\sum_{i: x_t^i = \texttt{MASK}} w^i \cdot \ell^i}{\sum_{i: x_t^i = \texttt{MASK}} w^i} \right]
\end{equation}
where $\ell^i = -\log p_\theta(x_0^i | \mathbf{x}_t, t)$ is the per-token cross-entropy loss and:
\begin{equation}
    w^i = 1 + \lambda_{\text{train}} \cdot \tilde{H}^i
\end{equation}

Here, $\lambda_{\text{train}} \geq 0$ controls the strength of entropy weighting. When $\lambda_{\text{train}} = 0$, this reduces to standard training. Importantly, we \textbf{detach} the entropy from the computation graph to prevent the model from trivially minimizing loss by increasing entropy.

\subsection{Entropy-Guided Distillation}

Our second and main contribution is entropy-guided distillation. Given a trained teacher model $p_T$ and a student model $p_S$, standard distillation minimizes:
\begin{equation}
    \mathcal{L}_{\text{KD}} = \mathbb{E} \left[ \text{KL}(p_T(\cdot | \mathbf{x}_t, t) \| p_S(\cdot | \mathbf{x}_t, t)) \right]
\end{equation}

Our key insight is that \textbf{teacher entropy identifies hard knowledge}. Tokens where the teacher is uncertain represent difficult cases that require more careful learning by the student. We propose:

\begin{equation}
    \mathcal{L}_{\text{EGD}} = \mathbb{E} \left[ \sum_{i: x_t^i = \texttt{MASK}} w_T^i \cdot \text{KL}(p_T^i \| p_S^i) \right]
\end{equation}
where the distillation weight is:
\begin{equation}
    w_T^i = 1 + \lambda_{\text{distill}} \cdot \tilde{H}_T^i
\end{equation}
and $\tilde{H}_T^i$ is the normalized entropy of the teacher's prediction at position $i$.

The complete distillation loss combines soft targets (KL divergence) and hard targets (ground truth):
\begin{equation}
    \mathcal{L}_{\text{total}} = \alpha \cdot \tau^2 \cdot \mathcal{L}_{\text{EGD}} + (1-\alpha) \cdot \mathcal{L}_{\text{hard}}
\end{equation}
where $\tau$ is the temperature for softening distributions, and $\mathcal{L}_{\text{hard}}$ is the entropy-weighted cross-entropy loss with ground truth labels.

\subsection{Model Architecture}

We use a Transformer encoder architecture similar to BERT \cite{devlin2019bert} and LLaDA \cite{nie2025llada}:

\begin{itemize}
    \item \textbf{Token Embedding}: Learnable embedding matrix $\mathbf{E} \in \mathbb{R}^{|\mathcal{V}| \times d}$
    \item \textbf{Positional Encoding}: Sinusoidal position embeddings
    \item \textbf{Time Embedding}: Sinusoidal time embedding followed by MLP, added to all positions
    \item \textbf{Transformer Layers}: Standard encoder layers with multi-head self-attention and feed-forward networks
    \item \textbf{Output Projection}: Linear layer projecting to vocabulary logits
\end{itemize}

% ============================================================================
\section{Theoretical Motivation}
% ============================================================================

\subsection{Why Entropy Weighting Helps}

Consider the distillation objective. Standard KL divergence treats all tokens equally:
\begin{equation}
    \mathcal{L}_{\text{KD}} = \sum_i \text{KL}(p_T^i \| p_S^i)
\end{equation}

However, not all tokens are equally informative. For tokens where the teacher is confident (low entropy), the distribution is peaked and easy to match. For high-entropy tokens, the distribution is spread across multiple candidates, representing nuanced knowledge that is harder to transfer.

By weighting with teacher entropy, we effectively implement \textbf{curriculum learning} within distillation: the student pays more attention to difficult cases where the teacher's knowledge is most valuable.

\subsection{Connection to Label Smoothing}

Interestingly, entropy-guided distillation has a connection to label smoothing \cite{muller2019does}. High-entropy teacher predictions naturally provide smoother targets than one-hot labels. By emphasizing these cases, we may achieve regularization benefits similar to label smoothing, potentially explaining why our student can surpass the teacher.

% ============================================================================
\section{Empirical Studies}
% ============================================================================

\subsection{Experimental Setup}

\textbf{Dataset.} We evaluate on WikiText-103 \cite{merity2017pointer}, a large-scale language modeling benchmark containing approximately 100M tokens from Wikipedia articles. We limit vocabulary size to 30,000 tokens for computational efficiency.

\textbf{Model Configuration.} Our model uses the following architecture:
\begin{itemize}
    \item Embedding dimension: 512
    \item Number of layers: 12
    \item Attention heads: 8
    \item Feed-forward dimension: 2048
    \item Dropout: 0.1
    \item Total parameters: \textbf{70.7M}
\end{itemize}

\textbf{Training Details.}
\begin{itemize}
    \item Sequence length: 128
    \item Batch size: 64
    \item Learning rate: $1 \times 10^{-4}$ with cosine annealing
    \item Teacher training: 20-30 epochs
    \item Student distillation: 10-15 epochs
    \item Diffusion steps (training): 1000
    \item Teacher sampling steps: 50
    \item Student sampling steps: 8
\end{itemize}

\textbf{Hyperparameters.}
\begin{itemize}
    \item $\lambda_{\text{train}} = 0.5$ (entropy training weight)
    \item $\lambda_{\text{distill}} = 1.0$ (entropy distillation weight)
    \item $\alpha = 0.7$ (soft target weight)
    \item $\tau = 2.0$ (distillation temperature)
\end{itemize}

\textbf{Evaluation Metric.} We report perplexity (PPL) on the validation set, computed as $\exp(\mathcal{L})$ where $\mathcal{L}$ is the average cross-entropy loss on masked positions at $t=0.5$.

\subsection{Main Results}

Table \ref{tab:main_results} presents our main experimental results comparing standard distillation (Base) with our entropy-guided approach (EGD-DLM).

\begin{table}[t]
\centering
\caption{Main Results on WikiText-103. $\lambda_{\text{train}}$ and $\lambda_{\text{distill}}$ denote entropy weights for training and distillation respectively. $\Delta$ PPL shows the relative change from teacher to student.}
\label{tab:main_results}
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & $\lambda_{\text{train}}$ & $\lambda_{\text{distill}}$ & \textbf{Teacher} & \textbf{Student} & $\Delta$ \textbf{PPL} & \textbf{Speedup} \\
\midrule
Base & 0.0 & 0.0 & 1178.97 & 1192.95 & +1.19\% & 8.93$\times$ \\
\textbf{EGD-DLM} & 0.5 & 1.0 & 1207.28 & \textbf{1193.30} & \textbf{-1.16\%} & 8.96$\times$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Key Findings:}

\begin{enumerate}
    \item \textbf{Standard distillation degrades performance.} The Base method shows that the student's PPL (1192.95) is 1.19\% worse than its teacher (1178.97), consistent with typical knowledge distillation behavior where some information is lost.

    \item \textbf{EGD-DLM enables student to surpass teacher.} Remarkably, our entropy-guided approach allows the student (1193.30) to achieve \textbf{1.16\% lower PPL} than its teacher (1207.28). This suggests that focusing on high-entropy tokens during distillation leads to more effective knowledge transfer and possible regularization benefits.

    \item \textbf{Comparable speedup.} Both methods achieve approximately \textbf{8.9$\times$ speedup} by reducing sampling steps from 50 to 8.

    \item \textbf{Final student performance is similar.} Both approaches converge to similar student PPL ($\sim$1193), but through different paths. EGD-DLM achieves this from a weaker teacher, demonstrating a ``remedial effect.''
\end{enumerate}

\subsection{Analysis of Entropy Dynamics}

Table \ref{tab:entropy_dynamics} shows the entropy evolution during distillation.

\begin{table}[t]
\centering
\caption{Entropy Dynamics During Distillation}
\label{tab:entropy_dynamics}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Teacher Entropy} & \textbf{Student Entropy (Start)} & \textbf{Student Entropy (End)} \\
\midrule
Base & 7.095 & 7.093 & 7.091 \\
EGD-DLM & 7.049 & 7.076 & 7.074 \\
\bottomrule
\end{tabular}
\end{table}

In both cases, student entropy converges toward teacher entropy, indicating successful knowledge transfer. The EGD-DLM teacher has slightly lower entropy (7.049 vs 7.095), suggesting more confident predictions despite higher PPL.

\subsection{Distillation Loss Analysis}

Figure~\ref{fig:soft_loss} illustrates the soft loss (KL divergence) evolution during distillation.

\begin{table}[t]
\centering
\caption{Soft Loss (KL Divergence) During Distillation}
\label{tab:soft_loss}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{Epoch 1} & \textbf{Epoch 5} & \textbf{Final} \\
\midrule
Base & 0.0003 & 0.0002 & 0.0000 \\
EGD-DLM & 0.0012 & 0.0007 & 0.0002 \\
\bottomrule
\end{tabular}
\end{table}

The EGD-DLM soft loss starts higher (0.0012 vs 0.0003) because entropy weighting emphasizes difficult tokens. However, it decreases more dramatically (83\% reduction) compared to Base, indicating that the student is successfully learning the challenging cases that entropy weighting prioritizes.

\subsection{Sampling Speed Comparison}

\begin{table}[t]
\centering
\caption{Sampling Speed Comparison (batch size = 4, sequence length = 128)}
\label{tab:speed}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{Steps} & \textbf{Time (s)} & \textbf{Speedup} \\
\midrule
Teacher & 50 & 0.30 & 1.0$\times$ \\
Student & 8 & 0.03 & \textbf{8.96$\times$} \\
\bottomrule
\end{tabular}
\end{table}

The student achieves nearly 9$\times$ wall-clock speedup, reducing generation time from 0.30s to 0.03s per batch.

% ============================================================================
\section{Discussion}
% ============================================================================

\subsection{Why Does the Student Surpass the Teacher?}

The phenomenon of student surpassing teacher in EGD-DLM is noteworthy. We hypothesize several contributing factors:

\begin{enumerate}
    \item \textbf{Implicit Regularization}: High-entropy teacher predictions provide softer targets, similar to label smoothing, which can improve generalization.

    \item \textbf{Focused Learning}: By weighting high-entropy tokens more heavily, the student dedicates more capacity to learning nuanced distinctions rather than memorizing easy cases.

    \item \textbf{Noise Reduction}: The distillation process may filter out noise in the teacher's predictions by emphasizing consistent patterns in uncertain regions.
\end{enumerate}

\subsection{Limitations}

\begin{enumerate}
    \item \textbf{Absolute PPL}: Our models achieve PPL $\sim$1200, significantly higher than state-of-the-art AR models (GPT-2: PPL $\sim$30). This reflects the general challenge of training discrete diffusion LMs, not a limitation of our distillation approach.

    \item \textbf{Scale}: We experiment with 71M parameter models. The effectiveness at larger scales (e.g., 7B+ parameters) remains to be validated.

    \item \textbf{Task Diversity}: We evaluate only on language modeling. Performance on downstream tasks requires further investigation.
\end{enumerate}

\subsection{Future Work}

\begin{enumerate}
    \item \textbf{Scaling}: Apply EGD-DLM to larger models like LLaDA-8B.
    \item \textbf{Adaptive $\lambda$}: Learn entropy weights dynamically rather than using fixed hyperparameters.
    \item \textbf{Multi-step Distillation}: Progressive distillation from 50$\rightarrow$25$\rightarrow$8 steps.
    \item \textbf{Task-Specific Evaluation}: Evaluate on reasoning, coding, and instruction-following benchmarks.
\end{enumerate}

% ============================================================================
\section{Conclusion}
% ============================================================================

We presented EGD-DLM, a novel framework for accelerating discrete diffusion language models through entropy-guided distillation. Our key insight is that teacher entropy identifies ``hard'' tokens requiring more attention during knowledge transfer. Experiments on WikiText-103 demonstrate that EGD-DLM achieves 8.96$\times$ speedup while uniquely enabling the student to surpass its teacher in perplexity—a phenomenon not observed in standard distillation. This work opens new directions for efficient diffusion-based text generation.

% ============================================================================
\section*{Acknowledgments}
% ============================================================================

This work was completed as part of STAT GR 5244: Applied Unsupervised Learning at Columbia University. We thank the course instructor and TAs for their guidance and feedback.

% ============================================================================
% References
% ============================================================================

\bibliographystyle{IEEEtran}
\begin{thebibliography}{99}

\bibitem{radford2019language}
A. Radford, J. Wu, R. Child, D. Luan, D. Amodei, and I. Sutskever, ``Language models are unsupervised multitask learners,'' \textit{OpenAI blog}, vol. 1, no. 8, p. 9, 2019.

\bibitem{touvron2023llama}
H. Touvron, T. Lavril, G. Izacard, et al., ``LLaMA: Open and efficient foundation language models,'' \textit{arXiv preprint arXiv:2302.13971}, 2023.

\bibitem{berglund2023reversal}
L. Berglund, M. Tong, M. Kaufmann, et al., ``The reversal curse: LLMs trained on `A is B' fail to learn `B is A','' \textit{arXiv preprint arXiv:2309.12288}, 2023.

\bibitem{ho2020denoising}
J. Ho, A. Jain, and P. Abbeel, ``Denoising diffusion probabilistic models,'' \textit{Advances in Neural Information Processing Systems}, vol. 33, pp. 6840--6851, 2020.

\bibitem{song2020score}
Y. Song, J. Sohl-Dickstein, D. P. Kingma, A. Kumar, S. Ermon, and B. Poole, ``Score-based generative modeling through stochastic differential equations,'' \textit{arXiv preprint arXiv:2011.13456}, 2020.

\bibitem{sahoo2024simple}
S. Sahoo, M. Arriola, Y. Schiff, et al., ``Simple and effective masked diffusion language models,'' \textit{arXiv preprint arXiv:2406.07524}, 2024.

\bibitem{lou2024discrete}
A. Lou, C. Meng, and S. Ermon, ``Discrete diffusion modeling by estimating the ratios of the data distribution,'' \textit{International Conference on Machine Learning}, 2024.

\bibitem{nie2025llada}
S. Nie, F. Zhu, Z. You, et al., ``Large language diffusion models,'' \textit{arXiv preprint arXiv:2502.09992}, 2025.

\bibitem{austin2021structured}
J. Austin, D. D. Johnson, J. Ho, D. Tarlow, and R. van den Berg, ``Structured denoising diffusion models in discrete state-spaces,'' \textit{Advances in Neural Information Processing Systems}, vol. 34, pp. 17981--17993, 2021.

\bibitem{song2023consistency}
Y. Song, P. Dhariwal, M. Chen, and I. Sutskever, ``Consistency models,'' \textit{International Conference on Machine Learning}, 2023.

\bibitem{salimans2022progressive}
T. Salimans and J. Ho, ``Progressive distillation for fast sampling of diffusion models,'' \textit{International Conference on Learning Representations}, 2022.

\bibitem{deschenaux2024sdtt}
J. Deschenaux, S. Gao, and A. Courville, ``Self-distillation through time for diffusion models,'' \textit{International Conference on Learning Representations}, 2025.

\bibitem{chen2025d2f}
Z. Chen, Y. Wu, et al., ``Diffusion LLMs can do faster-than-AR inference via discrete diffusion forcing,'' \textit{arXiv preprint arXiv:2508.09192}, 2025.

\bibitem{wang2025didi}
Y. Wang, et al., ``Ultra-fast language generation via discrete diffusion divergence instruct,'' \textit{arXiv preprint arXiv:2509.25035}, 2025.

\bibitem{han2024diffusion}
C. Han, et al., ``PLM-based discrete diffusion language models with entropy-adaptive Gibbs sampling,'' \textit{arXiv preprint arXiv:2411.06438}, 2024.

\bibitem{liu2025entropic}
Z. Liu, et al., ``Entropic time schedulers for generative diffusion models,'' \textit{arXiv preprint arXiv:2504.13612}, 2025.

\bibitem{devlin2019bert}
J. Devlin, M.-W. Chang, K. Lee, and K. Toutanova, ``BERT: Pre-training of deep bidirectional transformers for language understanding,'' \textit{NAACL-HLT}, 2019.

\bibitem{muller2019does}
R. M{\"u}ller, S. Kornblith, and G. E. Hinton, ``When does label smoothing help?,'' \textit{Advances in Neural Information Processing Systems}, vol. 32, 2019.

\bibitem{merity2017pointer}
S. Merity, C. Xiong, J. Bradbury, and R. Socher, ``Pointer sentinel mixture models,'' \textit{International Conference on Learning Representations}, 2017.

\end{thebibliography}

\end{document}
